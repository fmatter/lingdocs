Configuring ``pylingdocs``
=======================================

By default, ``pylingdocs`` uses the default config file, seen below.
The values therein can be overriden by a file ``pylingdocs.cfg`` in the directory where you are executing ``pylindocs``.

.. include:: ../src/pylingdocs/data/config.cfg
   :literal: