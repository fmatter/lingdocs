Configuring ``pylingdocs``
=======================================

By default, ``pylingdocs`` uses the config file shown below.
The values therein can be overridden by creating a file ``pylingdocs.cfg`` in the directory where you are executing ``pylingdocs``.

.. include:: ../src/pylingdocs/data/config.cfg
   :literal: